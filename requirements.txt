# Runtime dependencies (production / OpenShift image).
# Pinned to the environment that passes the surface_play test suite.
# Dev/test extras (pytest, matplotlib for probe scripts + visual-regression
# PNGs) live in requirements-dev.txt — NOT installed in the prod image.
#
# Dropped vs. the legacy list:
#   scs      — visibility LP now uses scipy.optimize.linprog(method="highs");
#              the wheel here was a numpy-1 build that fails under numpy 2.
#   matplotlib (+contourpy/cycler/fonttools/kiwisolver/pyparsing) — dev/test only.
#   Pillow, dill, pybind11 — not imported by the production pipeline.
asgiref==3.11.1
Django==4.2.30
gunicorn==20.1.0
llvmlite==0.46.0
mpmath==1.2.1
numba==0.64.0
numpy==2.4.3
scipy==1.17.1
sqlparse==0.4.2
sympy==1.14.0
triangle==20250106
tzdata==2022.1
whitenoise==6.12.0
